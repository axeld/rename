
resource app_signature "application/x-vnd.pinc.rename";

resource app_flags B_MULTIPLE_LAUNCH | B_BACKGROUND_APP;

resource app_version {
	major  = 0,
	middle = 1,
	minor  = 0,

	variety = B_APPV_FINAL,
	internal = 0,

	short_info = "batch renames files",
	long_info = "Renames multiple files at once, using regular expressions."
};

resource file_types message;
